machine:
  services:
    - docker
deployment:
  check:
    branch: master
    commands:
      - docker run -it sxend/ubuntu14.04:1.0.1 /bin/bash -c 'curl --insecure https://raw.githubusercontent.com/sxend/dotfiles/master/install.sh | bash'
