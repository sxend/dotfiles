machine:
  services:
    - docker
test:
  override:
    - docker run -it sxend/ubuntu14.04:latest /bin/bash -c 'curl --insecure https://raw.githubusercontent.com/sxend/dotfiles/master/install.sh | bash'
